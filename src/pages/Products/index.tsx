import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;

const Products: React.FC = () => {
  return (
    <>
      <Title level={2}>List of Products</Title>
      {/* Aqui virá a lista + busca + modal de cadastro + Buy */}
    </>
  );
};

export default Products;
